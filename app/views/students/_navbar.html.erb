<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
   <div class="container">
  <a class="navbar-brand" href="#">AbbesLaghrour</a>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Home', root_path, class:"nav-link" %>
      </li> <li class="nav-item active">
           <%= link_to 'About', about_path, class:"nav-link" %>
      </li>
      <li class="nav-item active">
          <%= link_to 'Contact', contact_path, class:"nav-link nvlk" %>
      </li>
      <li class="nav-item active">
          <%= link_to 'Dashboard', dashboard_path, class:"nav-link nvlk" %>
      </li>
    </ul>
  </div>
  <% if current_user %>
  Logged in as <%= current_user.Email %>
 <%= button_to "Logout" ,logout_path, class:"btn btn-danger", method: :delete %>
     <% else %>
     <%= button_to "Sign-Up" ,signup_path, class:"btn btn-primary mr-sm-4", method: :get %>
     <%= button_to "Login" ,login_path, class:"btn btn-info  my-2 my-sm-1", method: :get %>
     
     <% end %>
     </div>
</nav>
